import { PlayIcon } from './PlayIcon';
import type { IconProps } from './types';

export const PracticeIcon = ({ color, size = 20 }: IconProps) => {
  return <PlayIcon color={color} size={size} />;
};
